version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: protask
      POSTGRES_PASSWORD: protask123
      POSTGRES_DB: protask_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: